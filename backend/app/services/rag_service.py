"""
RAG Service (Placeholder)

This module will contain embedding-based retrieval logic in Phase 2.
Currently unused — the AI query flow uses direct document content.
"""


def retrieve_relevant_chunks(workspace_id: int, question: str) -> list:
    """
    Placeholder for Phase 2.

    Will:
    - Generate embedding for the question
    - Search vector DB for similar chunks
    - Return top-k relevant document chunks

    Currently returns empty list.
    """
    return []
